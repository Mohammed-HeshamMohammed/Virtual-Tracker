# Copy to .env.local for local dev (never commit .env.local).

# Dashboard Web — sign-in / trial CTAs (baked at build time for production)
# Local:  http://localhost:3000
# Prod:   https://app.myvirtualtracker.com
NEXT_PUBLIC_DASHBOARD_URL=

# Local production test after `npm run build` (optional; default 3001)
# PORT=3001
