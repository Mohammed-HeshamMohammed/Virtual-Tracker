# Dashboard app URL (set in deploy/.env as APP_PUBLIC_URL — baked into Landing-Web image)
# NEXT_PUBLIC_DASHBOARD_URL=https://app.yourdomain.com
#
# When unset, sign-in and trial CTAs stay on the landing site (/sign-in, /demo).
NEXT_PUBLIC_DASHBOARD_URL=
