# Coolify / Nixpacks — base directory: Landing-Web (prefer Dockerfile for standalone image)

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["node scripts/build.mjs"]

[start]
cmd = "node scripts/start.mjs"
