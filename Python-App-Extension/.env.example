# Virtual Tracker API (same as Frontend NEXT_PUBLIC_API_URL)
VT_API_URL=http://localhost:5712

# Web app URL for sign-in (/auth)
VT_WEB_URL=http://localhost:3000

# Local port for auth callback from browser (must match /auth?port=)
VT_AUTH_PORT=17389
