/** Max task cards shown in Tasks Breakdown (2-column grid). */
export const MAX_TASKS_BREAKDOWN = 6

/** Max project rows in Tasks per Project (matched panel height). */
export const MAX_TASKS_PER_PROJECT = 6

/** Shared height for below-the-fold task panels. */
export const TASK_PANEL_HEIGHT_CLASS = "h-[21.5rem]"
