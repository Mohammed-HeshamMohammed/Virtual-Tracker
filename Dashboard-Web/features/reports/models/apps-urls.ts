export interface AppUsageRow {
  memberId: string
  memberName: string
  appName: string
  durationHms: string
}

export interface UrlUsageRow {
  memberId: string
  memberName: string
  domain: string
  durationHms: string
}
