// Global providers — theme + auth session.

export { AuthProvider, useAuth } from "@/shared/providers/auth/auth-context"
export { ThemeProvider, useTheme } from "@/shared/providers/theme/theme-context"
