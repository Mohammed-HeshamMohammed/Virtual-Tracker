# Logs
*.log
npm-debug.log*

# Dependencies
node_modules/

# Build
dist/
src-tauri/target/
.vite/

# Env / secrets
.env
.env.production
agent-store.json
preferences.json
tracker-progress.json
# Runtime queue of activity events not yet delivered to the backend (dev
# builds run from this directory write it here instead of the real
# per-user data dir, same as classifications.json below). Not source.
pending-events.jsonl

# Editor
.DS_Store
.idea/
.vscode/
# Tauri private key
updater-key.pem
# Runtime classification-name cache written by capture/classification_cache.rs
# (dev builds run from this directory write it here instead of the real
# per-user data dir). Not source, never commit.
classifications.json

# Plaintext handoff of the updater signing key's password - move to a password
# manager and delete this file. Never commit it.
updater-key-password.txt
